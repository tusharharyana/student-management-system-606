import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#0F172A",
    },

    secondary: {
      main: "#1E293B",
    },

    background: {
      default: "#F8FAFC",
    },
  },
});

export default theme;
